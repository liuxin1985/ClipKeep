# 更新日志

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/) 与语义化版本。

## [1.2.0] - 2026-09-26

### 新增
- **标签管理面板**：弹窗工具栏新增 🏷 按钮，列出每个标签的收藏数量，支持
  重命名、合并（自动去重）与删除；改名与合并走 background 的 `clipkeep:tag-op`
  批量落盘，一次改动全站生效。
- **搜索命中高亮**：搜索关键词在卡片正文、备注与来源标题中以 `<mark>` 标出，
  正则元字符已转义，大小写不敏感，切换筛选后仍保持高亮。
- **回顾设置**：弹窗新增 ⚙ 设置面板，可调「每日回顾上限」（1–200）与
  「间隔倍率」（0.5× 快速 / 1× 标准 / 2× 轻松）；非法输入自动回落到默认值，
  待回顾数量超过上限时徽标显示 `上限/总数`。
- **恢复冲突提示**：导入备份前给出差异预览（新增 / 相同 / 仅本地），
  可选择「合并」「覆盖本地」或「取消」；覆盖会额外确认本地独有数据，
  备份与本地一致时直接提示无需恢复。
- **快捷键秒存**：`Alt+Shift+K` 直接收藏当前页面选中的文字，无需右键或点工具条；
  service worker 只负责把消息转投当前标签页，未选中文字时页面内给出 toast 提示。

### 变更
- 弹窗底部操作区拆分为「⬇ 一键导出 Markdown」与「备份 / 恢复 / 清空」两组，
  导出跟随当前视图，备份导出收藏 + 高亮的完整 JSON。
- 「清空全部」现在会明确提示高亮与批注不受影响。

### 修复
- 修复收藏 / 批注卡片按钮选择器拼写错误（`clipkeep-cancel` → `clipkeep-btn-cancel`），
  该问题会导致卡片「保存 / 取消」点击无响应。
- 修复回顾设置中非法上限（如 `0`）被直接写入存储的问题，现在先夹取再落盘。

### 测试
- 断言从 58 项扩展到 **132 项**，新增快捷键消息链路、标签重命名 / 合并 / 删除、
  搜索命中高亮、上限与倍率夹取、恢复三种选择与幂等性、收藏卡片保存与取消回归用例。
- CI 描述文件见 `docs/ci/workflow-test.yml`（复制到 `.github/workflows/test.yml` 即生效）。

## [1.1.0] - 2026-09-24

### 新增
- **划词高亮**：浮动工具条新增 🖍 按钮，选中文字即刻涂色；高亮写入 `storage.local`，
  重新打开页面自动按文本匹配重放（`Range` + `TreeWalker`），跨标签页实时同步。
- **原文批注**：浮动工具条新增 ✎ 按钮，批注以粉色高亮呈现并在悬停时显示；
  点击已有高亮可修改批注，输入 `d` 删除该高亮。支持黄 / 绿 / 粉 / 蓝四色。
- **每日回顾（间隔重复）**：弹窗新增「🔁 回顾」标签页，Leitner 六盒（0/1/3/7/21/90 天），
  忘记 / 记得 / 简单三档评分驱动升降盒，标签页标题带待回顾红点徽标。
- **JSON 备份 / 恢复**：弹窗底部「备份」导出收藏 + 高亮的单个 JSON；
  「恢复」按 id 合并（本地优先、缺失新增），重复导入幂等，坏文件安全拒绝。
- **自动化测试**：`test/clipkeep.test.mjs` 用 jsdom 直接跑真实的
  background / popup / content 代码，58 项断言覆盖消息路由、复习排期、备份恢复往返、高亮重放与删除。
  `npm test` / `npm run check` 一条命令验证。

### 变更
- 弹窗从单列表升级为「收藏 / 回顾」双视图，底部操作区改为「备份 · 恢复 · 清空全部」。
- 「清空全部」只清收藏，高亮与批注独立保存不受影响。
- 导出按钮跟随当前视图：收藏视图按搜索与标签筛选结果导出。

## [1.0.0] - 2026-09-23

### 新增
- 划词浮动工具条：选中文字一键收藏，支持备注与标签。
- 右键菜单：收藏选中内容 / 净化阅读本页。
- 极简列表弹窗：搜索、排序、标签筛选、复制、删除。
- Markdown 导出：单条 + 批量下载 `.md`。
- 净化阅读模式：移除广告 / 侧边栏 / 导航，支持收藏全文，Esc 退出。
- 深色模式，偏好本地持久化。
- 跨浏览器适配：Chrome / Edge（Manifest V3）+ Safari 16.4+ 适配清单。
- 完整开发规划、安装、贡献与 Issue 模板文档。
