# ClipKeep 开源浏览器插件开发与涨星规划

## 项目定位

跨 Chrome、Edge、Safari 的轻量划词收藏插件，纯前端离线运行，无后端无账号。
学生整理网课重点、上班族留存周报素材都能用，主打**一键划词存本地、导出 Markdown、净化网页阅读**。

## 核心功能清单（v1.0 已完成）

1. 划词监听 + 浮动工具条，点击收藏到 `storage.local`。
2. 快速收藏卡片：支持备注、标签。
3. 右键菜单：收藏选中内容 / 净化阅读本页。
4. 极简列表弹窗：展示已存内容、搜索、排序、标签筛选。
5. Markdown 导出：单条 + 批量。
6. 净化阅读模式：移除广告 / 侧边栏 / 导航，只留正文，可收藏全文。
7. 标签分类、深色模式、一键复制单条。

## 核心功能清单（v1.1 已完成）

8. 划词高亮：四色涂色，落盘后可跨标签页同步，重开页面按文本自动重放。
9. 原文批注：粉色高亮 + 悬停显示，点击高亮可改可删。
10. 每日回顾：Leitner 六盒间隔重复（0/1/3/7/21/90 天），待回顾红点徽标。
11. JSON 备份 / 恢复：收藏 + 高亮单文件导出，按 id 幂等合并。
12. 自动化测试：jsdom 直跑真实代码，58 → 132 项断言，`npm test` 一条命令。

## 核心功能清单（v1.2 已完成）

13. 标签管理面板：🏷 列出每个标签的数量，重命名 / 合并（去重）/ 删除一次批量生效。
14. 搜索命中高亮：关键词在正文、备注、来源标题里以 `<mark>` 标出，正则元字符转义。
15. 回顾设置：⚙ 面板调「每日上限」1–200 与「间隔倍率」0.5× / 1× / 2×，非法值夹取回落。
16. 恢复冲突提示：导入前 diff 预览（新增 / 相同 / 仅本地），三选一「合并 / 覆盖本地 / 取消」，
    覆盖额外确认，备份与本地一致时直接提示无需恢复。
17. 快捷键秒存：`Alt+Shift+K`（`commands` + service worker 转投标签页），无需右键或点按钮。
18. 修复：收藏 / 批注卡片按钮选择器拼错导致「保存 / 取消」点击无响应；设置非法值被直写存储。

## 持续集成（v1.1.0 已配）

- 工作流 `docs/ci/workflow-test.yml`（复制为 `.github/workflows/test.yml` 即生效）：
  `npm run check` → manifest JSON/MV3 校验 → 离线（无 CDN）校验 → `npm test`。
- 未直接放入 `.github/` 的原因：当前推送用的 OAuth 令牌没有 `workflow` 作用域，需 `gh auth refresh -s workflow`。

## 核心功能清单（v1.3 规划）

19. 导出模板自定义（标题格式、是否带来源、Obsidian front-matter）。
20. 图片与链接剪藏。
21. 回顾统计热力图（每日复习量可视化）。
22. 英文 UI 与国际化文案层。
23. 真实录屏替换 `docs/demo.gif` 的示意动图。


## 技术选型

- **Manifest V3**（Chrome / Edge）+ Safari 16.4+ 适配层。
- 原生 JavaScript，无框架、无构建工具。
- 存储：`chrome.storage.local` / `browser.storage.local`。
- 高亮重放：`Range.surroundContents` + `TreeWalker` 文本匹配（无坐标依赖，抗排版变化）。
- 回顾调度：Leitner 盒 + `due` 时间戳，字段内嵌在收藏项里，天然随备份走。
- UI：手写 CSS，清爽卡片风（避免 MV3 CSP 对 CDN 的限制）。
- 跨浏览器适配：`const API = browser || chrome` 统一入口。
- 测试：jsdom（仅 devDependency，扩展产物零依赖）。

## 30 天里程碑（满足月内维护记录）

### 1. 第 1–7 天 基础版上线
**AI 开发 Prompt：**
> 生成基于 Manifest V3 的 Chrome/Edge 插件，兼容 Safari 特殊适配层，实现划词监听、点击弹窗保存到 localStorage、极简列表展示已存内容，UI 用清爽卡片风。

**提交记录：** Day3 搭仓库初始化，Day7 提交基础可用版。

### 2. 第 8–18 天 核心功能迭代
**AI 开发 Prompt：**
> 给现有插件加单条/批量导出 Markdown、一键净化当前网页移除广告侧边栏，适配 Safari 的 tab 权限。

**提交记录：** 每 3 天发一次小优化 Commit（修复划词误触发、调整导出格式）。

### 3. 第 19–30 天 吸星小更新 + 维护
**AI 开发 Prompt：**
> 加标签分类、深色模式、一键复制单条内容，写 3 条常见问题 Issue 回复模板。

**提交记录：** 每 2 天提交一次小补丁，主动回复模拟 Issue，凑够连续维护痕迹。

### 4. v1.1 复利更新（让老用户回来）
**AI 开发 Prompt：**
> 给现有插件加：划词高亮与原文批注（刷新后自动重放、点击可改删）、基于 Leitner 盒的每日回顾、
> 收藏与高亮的 JSON 备份/恢复（按 id 幂等合并），并用 jsdom 写一套真实代码的自动化测试。

**提交记录：** 2026-09-24 一次性交付三功能 + 测试 + 文档，版本号升到 1.1.0。

### 5. v1.2 效率打磨（让高频动作更快）
**AI 开发 Prompt：**
> 给现有弹窗加标签管理面板（重命名 / 合并 / 删除，批量落盘）、搜索命中高亮、回顾设置（每日上限 + 间隔倍率）、
> 导入备份前先给 diff 预览并让用户在合并 / 覆盖 / 取消间选择，再加 `Alt+Shift+K` 秒存选区；每项都补 jsdom 断言。

**提交记录：** 2026-09-26 五功能 + 132 项断言 + 文档一起交付，版本号升到 1.2.0。
顺带修掉两处真实缺陷：卡片按钮类名选择器拼错（保存 / 取消点了没反应）、设置非法值被直写存储。

### 6. v1.3 规划（持续维护）
- 导出模板自定义、图片 / 链接剪藏、回顾热力图、英文 UI、真实录屏演示。

## 涨星 README 模板要点

- 开头放演示动图，展示划词存内容（当前为 mockup，脚本 `docs/gen_demo_gif.py`，待真实录屏替换）。
- 加粗「**零账号、纯本地、跨三浏览器**」。
- 附一键安装步骤。
- 结尾加 Contribute 指引，引导点 Star。

## 验收标准（Definition of Done）

- [x] `extension/` 目录可被 Chrome「加载已解压的扩展程序」直接加载。
- [x] 划词浮动条出现且可收藏，收藏后弹窗可见。
- [x] 净化阅读可进入 / 退出（Esc 或退出按钮）。
- [x] 导出 Markdown 可下载 .md 文件。
- [x] 标签筛选、搜索、排序、深色模式、复制、删除均可用。
- [x] 无任何后端请求、无第三方依赖。
- [x] 高亮在页面重开后自动恢复，点击可改可删，跨标签页实时同步。
- [x] 回顾评分后下次到期时间正确（忘记→立即，记得→+1 盒，简单→+2 盒）。
- [x] 备份 → 清空 → 恢复数据完全一致，重复恢复不产生重复项，坏文件不炸。
- [x] 恢复导入有差异预览，「合并 / 覆盖本地 / 取消」三种选择行为可分别验证。
- [x] 标签重命名 / 合并 / 删除批量生效，合并后同一收藏不出现重复标签。
- [x] 搜索关键词在卡片里标黄，特殊字符（`(` `*` `.`）不会导致报错。
- [x] 每日上限与间隔倍率设置生效，非法输入回落默认值且不写入脏数据。
- [x] `Alt+Shift+K` 在普通网页把选区直接入库；未选中文字时只提示、不报错。
- [x] `npm run check` 与 `npm test`（132 项断言）全绿。
